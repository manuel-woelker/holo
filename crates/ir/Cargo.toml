[package]
name = "holo-ir"
version = "0.1.0"
edition = "2021"
description = "Typed intermediate representation for holo"
license = "MIT"
publish = false

[dependencies]
holo-ast = { path = "../ast" }
holo-base = { path = "../base" }
