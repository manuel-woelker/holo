[package]
name = "holo-token"
version = "0.1.0"
edition = "2021"
description = "Token types used by the holo language frontend"
license = "MIT"
publish = false

[dependencies]
holo-base = { path = "../base" }
