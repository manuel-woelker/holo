[package]
name = "holo-engine"
version = "0.1.0"
edition = "2021"
description = "Incremental compilation engine for holo"
license = "MIT"
publish = false

[dependencies]
holo-base = { path = "../base" }
